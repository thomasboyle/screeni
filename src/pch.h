// Precompiled header for the Qt-based UI. Compiled once per build; every other
// translation unit gets /FI to this file, so the Qt headers are parsed only a
// single time. Keep it free of anything project-specific.
#ifndef SCREENI_PCH_H
#define SCREENI_PCH_H

#include <QtCore>
#include <QtGui>
#include <QtWidgets>
#include <QtNetwork>

#endif // SCREENI_PCH_H